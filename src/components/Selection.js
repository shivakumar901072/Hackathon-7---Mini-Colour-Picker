import React from 'react';

const Selector = (props) => {
  const { applyColor,changeApplyColor} = props
  
  return (
      <div className="fix-box">
   
  )
    <h5 className="subheading">{Selection}</h5>
    </div>
  )
}
export default Selector;